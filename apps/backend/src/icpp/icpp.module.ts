import { Module } from '@nestjs/common';

/**
 * Placeholder — Sprint 9 + 10:
 *   Motor de cálculo do ICPP-SP.
 *   AS REGRAS SÓ SERÃO IMPLEMENTADAS APÓS VALIDAÇÃO DE PV1–PV10.
 *   A engine lerá: icpp_methodologies.regras_* JSONB → basket_weights → item_prices
 *   e produzirá icpp_results + category_results CONGELADOS (hash_calculo + versao_motor).
 */
@Module({})
export class IcppModule {}
